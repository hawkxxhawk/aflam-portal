{
  "name": "r-movies",
  "version": "1.0.0",
  "description": "R-Movies IPTV Application",
  "main": "index.html",
  "scripts": {
    "build:android": "mkdir -p www && cp *.html *.js *.css *.json www/ && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
